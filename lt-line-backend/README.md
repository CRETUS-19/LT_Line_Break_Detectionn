# LT Line Monitoring Backend

This is the Node.js backend for the LT Line Break Detection System.

## Features
- Receives pole sensor data via REST API
- Stores live data in Firebase Realtime Database
- Stores historical data in MongoDB
- Detects LT line faults
- Ready for Cloud Run deployment

## Run Locally
npm install
npm start